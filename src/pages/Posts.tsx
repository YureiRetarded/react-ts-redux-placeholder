import React, {FC} from 'react';
import PostsList from "../components/PostsList";

const Posts:FC = () => {
    return (
        <div>
            <PostsList/>
        </div>
    );
};

export default Posts;