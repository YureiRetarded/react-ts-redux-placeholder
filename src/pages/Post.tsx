import React, {FC} from 'react';
import PostBlock from "../components/PostBlock";

const Post:FC = () => {
    return (
        <div>
            <PostBlock/>
        </div>
    );
};

export default Post;